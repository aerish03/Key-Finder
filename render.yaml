services:
  - type: web
    name: songbpm-quick-finder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: CACHE_TTL_SECONDS
        value: "900"
